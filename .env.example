# ============================================================================
# 币安期货AI分析系统 - 环境变量配置模板
# ============================================================================
# 说明：复制本文件为 .env，然后填入实际的配置值
# 使用方法：cp .env.example .env
#
# 注意：
# 1. .env 文件包含敏感信息，请勿提交到版本控制系统
# 2. 基于已添加到 .gitignore 中，自动被Git忽略
# 3. 不同环境（开发/测试/生产）使用不同的.env文件
# ============================================================================

# ============================================================================
# 1. 币安期货 (Binance Futures) 配置 - 必需
# ============================================================================

# 币安API密钥
# 获取方法: https://www.binance.com -> 账户 -> API管理
# 说明: 用于身份验证，只读权限即可
BINANCE_API_KEY=your_binance_api_key_here

# 币安API密钥秘密
# 获取方法: 同上
# 说明: 用于签名请求，务必保管妥善
BINANCE_API_SECRET=your_binance_api_secret_here

# ============================================================================
# 2. DeepSeek AI 配置 - 必需
# ============================================================================

# DeepSeek API密钥
# 获取方法: https://api.deepseek.com -> 创建API Key
# 说明: 用于调用AI分析接口，需要充足的账户余额
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here

# ============================================================================
# 3. 应用运行配置 - 可选，有默认值
# ============================================================================

# 数据刷新间隔 (秒)
# 说明: K线数据、仓位信息的刷新频率
# 推荐值:
#   - 低配置服务器: 5-10
#   - 标准配置: 2-3
#   - 高配置: 1-2
# 默认值: 2
REFRESH_INTERVAL=2

# K线时间框架
# 说明: 显示的蜡烛线周期
# 可选值: 1m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M
# 推荐值: 1h (1小时)
# 默认值: 1h
KLINE_INTERVAL=1h

# ============================================================================
# 4. Streamlit 服务器配置 - 可选
# ============================================================================

# 服务器绑定地址
# 说明: 应用监听的网络接口
# 值说明:
#   - 0.0.0.0: 监听所有接口（推荐用于服务器）
#   - 127.0.0.1: 仅本地访问（开发环境）
#   - 192.168.x.x: 特定IP （局域网）
# 默认值: 0.0.0.0
SERVER_ADDRESS=0.0.0.0

# 服务器端口
# 说明: 应用运行的HTTP端口
# 说明: 
#   - 生产环境建议使用 8501
#   - 需要管理员权限使用 < 1024 的端口
# 默认值: 8501
SERVER_PORT=8501

# ============================================================================
# 5. 应用显示配置 - 可选
# ============================================================================

# 应用标题
# 说明: 浏览器标签页显示的标题
# 默认值: 币安期货AI分析系统
APP_TITLE=币安期货AI分析系统

# ============================================================================
# 6. 数据库配置 - 可选
# ============================================================================

# SQLite数据库文件路径
# 说明: 存储分析历史和缓存数据
# 可以是相对路径或绝对路径
# 默认值: ./analysis_cache.db
DATABASE_PATH=./analysis_cache.db

# ============================================================================
# 7. 调试和日志配置 - 可选
# ============================================================================

# 调试模式
# 说明: 启用详细日志输出和错误堆栈跟踪
# 值说明: true / false
# 注意: 生产环境务必设置为 false
# 默认值: false
DEBUG_MODE=false

# ============================================================================
# 8. 高级配置 - 可选，一般不需要修改
# ============================================================================

# AI分析冷却时间 (秒)
# 说明: 同一个交易对两次分析的最小间隔
# 默认值: 300 (5分钟)
# AI_ANALYSIS_COOLDOWN=300

# 后台分析线程运行间隔 (秒)
# 说明: 后台自动更新分析的运行频率
# 默认值: 300 (5分钟)
# BACKGROUND_ANALYSIS_INTERVAL=300

# 历史数据保留天数
# 说明: 自动删除此天数之外的历史数据
# 默认值: 7
# DATA_RETENTION_DAYS=7

# ============================================================================
# 配置验证清单
# ============================================================================
# ✓ BINANCE_API_KEY - 已填入
# ✓ BINANCE_API_SECRET - 已填入  
# ✓ DEEPSEEK_API_KEY - 已填入
# ✓ 其他配置 - 已根据需求自定义
#
# 验证步骤:
# 1. 保存 .env 文件
# 2. 运行: python -c "from config import *; print('Config loaded successfully')"
# 3. 没有错误即表示配置正确
# ============================================================================

# ============================================================================
# 常见问题
# ============================================================================
# Q: 如何获得币安API密钥？
# A: 访问 https://www.binance.com，登录账户，进入 账户 -> API管理
#    创建新API Key，启用"期货权限"，禁用"提现权限"
#
# Q: 如何获得DeepSeek API密钥？
# A: 访问 https://api.deepseek.com，注册账户，在个人中心创建API Key
#    确保账户有充足的余额用于API调用
#
# Q: 生产环境推荐配置是什么？
# A: REFRESH_INTERVAL=2, KLINE_INTERVAL=1h, DEBUG_MODE=false
#    SERVER_ADDRESS=0.0.0.0, SERVER_PORT=8501
#
# Q: 能在多台服务器上使用同一个API密钥吗？
# A: 可以，但建议为每台服务器创建不同的API Key以便管理和审计
#
# Q: 数据库文件会变得很大吗？
# A: 不会，系统自动清理7天外的数据，数据库大小保持在较小范围内
# ============================================================================
